# 训练数据生成器 6 大问题及对训练的影响分析

## 问题总览

| # | 问题 | 严重程度 | 不修复的后果 |
|---|---|---|---|
| 1 | SFT 数据无轨迹结构 | **致命** | SFT 训练完全无法启动 |
| 2 | 随机单号与资产脱节 | **致命** | 100% 训练样本金标错误 |
| 3 | logistics.jsonl 缺 status_cn | **致命** | 运行时 KeyError 崩溃 |
| 4 | 模板仅 14 句 | 严重 | 模型过拟合，泛化能力差 |
| 5 | 对抗题仅 4 种 | 严重 | 安全护栏缺失 9 类场景 |
| 6 | Teacher 默认模型指错 | 中等 | 加载 4B 小模型，质量远低于预期 |

---

## 问题 #1：SFT 数据无轨迹结构

### 现状

`sft_train.jsonl` 每行只有：
```json
{"id": "single_001817", "type": "single_tool", "query": "请帮我搜索 clothing 相关的商品", "required_tools": ["text_search"]}
```

没有 `messages` 数组、没有 `tools` schema、没有 Observation 记录。

### 为什么必须有轨迹结构

SFT（Supervised Fine-Tuning）的本质是**教模型模仿正确行为序列**。ms-swift / transformers 的 SFTTrainer 要求输入是完整的多轮对话：

```
system → user → assistant(tool_calls) → tool(Observation) → assistant(终答)
```

这条链路中：
- **system**：告诉模型角色定位和行为规范
- **user**：用户的真实问题
- **assistant(tool_calls)**：模型需要学会**何时调用工具、调哪个工具、传什么参数**
- **tool(Observation)**：工具返回的真实数据，模型需要学会**基于 Observation 生成终答**
- **assistant(终答)**：基于 Observation 的准确回答

### 不修复的影响

1. **SFT 训练完全无法启动**：SFTTrainer 解析不到 `messages` 字段，直接报错或跳过所有样本
2. **即使强行训练也无意义**：只给 `query + required_tools`，模型学到的是"看到问题就报工具名"，而不是"调用工具→解析结果→生成回答"的完整链路
3. **GRPO 强化学习也无法使用**：GRPO 需要模型先生成完整轨迹，再用 Reward 打分。没有轨迹结构，GRPO 的采样和评分无从谈起

### 修复方案

重写 `generate_all` 输出逻辑：逐工具调用 `train_executor` 真实执行，拼装 `{messages, tools}` 格式。

---

## 问题 #2：随机单号与资产脱节

### 现状

```python
params["order_id"] = f"ORD{random.randint(10000000, 99999999)}"  # ORD16389494
```

而 `logistics.jsonl` 的真实单号是 `ORD00000001` ~ `ORD00005000`。

### 为什么必须用真实 ID

训练数据的金标（ground truth）是模型学习的"标准答案"。金标包含：
- **工具调用参数**：如 `{"order_id": "ORD00000889"}`
- **期望 Observation**：如 `{"status": "signed", "status_cn": "已签收"}`
- **期望终答**：如 "订单 ORD00000889 的物流状态为：已签收"

当使用随机单号时：
- 执行器 `_tool_query_logistics("ORD16389494")` 返回 `NOT_FOUND`
- 金标终答却写 "订单 ORD16389494 的物流状态为：已签收"
- **Observation 和终答完全矛盾**

### 不修复的影响

1. **100% 金标错误**：涉及物流查询的样本，Observation 是 `NOT_FOUND`，但金标终答是正常物流状态。模型学到的是"工具说查不到，但我应该说已签收"——这是**教模型编造数据**
2. **模型学会幻觉**：SFT 的核心是让模型学会"基于工具返回结果回答"。如果金标本身就是编造的，模型会学到"不需要看工具结果，直接编一个回答就行"
3. **GRPO 奖励崩溃**：GRPO 用金标对比模型输出打分。金标是错的，正确的输出反而得低分，模型被反向训练
4. **评测失真**：评测集用同样的随机单号，模型在真实场景中遇到 `ORD00000889` 时完全无法正确处理

### 修复方案

从 `logistics.jsonl` 中提取真实 `order_id` 列表，生成时 `random.choice()` 采样。

---

## 问题 #3：logistics.jsonl 缺 `status_cn` 字段

### 现状

`logistics.jsonl`：
```json
{"order_id": "ORD00000001", "status": "signed", "trajectory": [...]}
```

`train_executor.py:426`：
```python
"status_cn": track["status_cn"]  # KeyError!
```

### 不修复的影响

1. **运行时崩溃**：任何调用 `_tool_query_logistics` 的操作都会抛 `KeyError: 'status_cn'`，训练数据生成直接中断
2. **级联故障**：生成器崩溃 → 没有训练数据 → SFT/GRPO 训练全部无法启动
3. **即使 catch 住也有问题**：如果用 `try/except` 吞掉错误，这些样本会被跳过，导致物流类训练数据严重不足

### 修复方案

在 `_tool_query_logistics` 中用已有的 `STATUS_CN` 字典做转换：
```python
"status_cn": STATUS_CN.get(track["status"], track["status"])
```

---

## 问题 #4：模板仅 14 句

### 现状

| 类别 | 模板数 |
|---|---|
| 单工具（物流/防伪/搜索/退款/图片） | 5 |
| 多工具（退款/比价/签收验货） | 3 |
| 多轮（物流→验货→退款 / 搜索→比价→建议） | 2 |
| 对抗（诱导跳过验证） | 4 |
| **合计** | **14** |

### 为什么需要更多模板

SFT 训练的本质是**让模型从多种表达方式中学会同一个意图**。如果训练数据只有 14 种问法：

1. **过拟合**：模型只认识这 14 种句式，换个说法就不会了。用户说 "我的包裹到哪了" 模型能处理，但说 "快递走到哪一步了" 就不行
2. **缺乏鲁棒性**：真实用户输入有大量口语化、省略、错别字、方言表达，14 句模板无法覆盖
3. **售后话术类完全缺失**：话术对抗、情绪安抚、升级转人工等关键场景没有训练数据，模型遇到这类情况会乱回答

### 不修复的影响

1. **线上准确率低**：模型在 benchmark 上可能表现不错，但真实用户稍微换个说法就无法处理
2. **安全护栏缺失**：没有话术对抗训练，模型容易被用户诱导跳过验证、泄露信息
3. **客户体验差**：遇到情绪激动的用户，模型不会安抚、不会升级转人工，只会重复标准话术

### 修复方案

扩充至 50+ 模板，覆盖：
- 售后话术类（话术对抗、情绪安抚、升级转人工）
- 模糊指代类（"那个订单"、"上次买的东西"）
- 多意图混杂类（"查物流顺便帮我退款"）
- 跨工具依赖类（OCR → 识别单号 → 查物流）

---

## 问题 #5：对抗题仅 4 种（PRD 要求 10 类 × 360 题）

### 现状

4 种对抗模板全部属于 "诱导跳过验证" 这一个大类。

PRD 18 5.5 要求的 10 类：

| 类别 | 是否覆盖 | 不覆盖的风险 |
|---|---|---|
| 诱导跳过验证 | ✅ | - |
| 拒绝认错/甩锅第三方 | ❌ | 模型跟着用户甩锅，损害品牌形象 |
| 情绪失控要挟 | ❌ | 模型不会安抚，激化矛盾 |
| 恶意利用政策 | ❌ | 被薅羊毛，造成经济损失 |
| 图文不符/误导 | ❌ | 用假图骗退款成功 |
| 模糊/歧义输入 | ❌ | 模型猜测错误，答非所问 |
| 信息缺失需澄清 | ❌ | 模型编造数据回答 |
| 多轮编造/幻觉 | ❌ | 模型配合用户编造虚假信息 |
| Prompt 注入 | ❌ | 模型被劫持，执行恶意指令 |
| 边界/极端场景 | ❌ | 处理超时订单/停产商品时崩溃 |

### 不修复的影响

1. **安全护栏千疮百孔**：10 类攻击场景只有 1 类被防御，其余 9 类模型完全不设防
2. **线上事故频发**：用户用情绪要挟就能拿到退款，用假图就能通过验货，Prompt 注入能劫持模型
3. **合规风险**：恶意利用政策、图文不符等场景如果没有正确处理，可能涉及消费者权益纠纷

### 修复方案

每类 360 题，共 3600 题。每类定义：
- 攻击模板（3+ 种句式）
- 正确行为（`refuse` / `ask_user` / `transfer_to_human`）
- 正确响应模板

---

## 问题 #6：Teacher 默认模型指错

### 现状

```python
DEFAULT_MODEL = str(_ROOT / "models" / "Qwen3.5-4B-Q4_K_M.gguf")  # 2.55GB
```

但脚本的 OOM 预算（`VRAM_WEIGHT = 18GB`）和注释全部围绕 27B 模型设计。

### 不修复的影响

1. **加载 4B 小模型**：Qwen3.5-4B 的推理能力远弱于 Qwen3.8-27B，生成的轨迹质量低
2. **KV cache 预算被高估**：脚本按 17GB 权重预留显存，实际 4B 只需 2.5GB，导致 batch_size 被不必要地压缩
3. **Teacher 轨迹质量差**：4B 模型生成的工具调用链容易出错（调错工具、传错参数），四道闸过滤后通过率低，浪费 GPU 时间

### 修复方案

```python
DEFAULT_MODEL = str(_ROOT / "models" / "Qwen3.8-27B-UD-Q4_K_XL.gguf")
```

---

## 总结：不修复的级联影响

```
问题 #3 (KeyError)
    ↓ 崩溃
问题 #1 (无轨迹) + 问题 #2 (随机单号)
    ↓ 即使绕过崩溃，数据不可用
SFT 训练无法启动
    ↓
GRPO 无法启动（依赖 SFT 基座）
    ↓
问题 #4 (模板少) + 问题 #5 (对抗少)
    ↓ 即使训练了，模型质量差
线上准确率低 + 安全护栏缺失
    ↓
问题 #6 (Teacher 指错)
    ↓ Teacher 轨迹质量差
高质量训练数据无法产出
```

**结论**：6 个问题形成闭环，任何一个不修复都会导致训练流程断裂或产出质量不可接受。其中 #1 #2 #3 是阻塞性 bug，必须首先修复；#4 #5 是功能性缺失，决定模型质量上限；#6 是配置错误，影响 Teacher 产线效率。
