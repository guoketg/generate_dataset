# 项目进度（2026-08-21）

更新时间: 2026-08-21

## 当前状态

**验收状态: 11/12 检查通过，1项失败（多样性检查）**

- 多样性检查: 48-51%（需要 ≥60%）
- 差距: 需要约 1262 个更多唯一查询

## 关键问题（用户指出）

**⚠️ 核心问题：当前训练数据基于模板生成，非 LLM 生成**

用户指出：SFT和GRPO的数据应该由Qwen3.8-28B-4bit生成，而不是基于模板。当前实现完全是模板化和随机化的，模型学到的就只是模板模式，而非真实的语言表达。

**问题分析**：
1. 当前`gen_training_data.py`脚本完全基于预定义模板和随机化生成数据
2. 没有使用任何LLM模型来生成自然语言查询
3. 模型会学到模板模式，而非真实的用户表达方式
4. 这会导致模型泛化能力差，无法处理真实世界的多样性表达

**需要的解决方案**：
1. 修改数据生成流程，使用Qwen3.8-28B-4bit生成自然语言查询
2. 保留工具调用和轨迹结构，但让LLM生成用户查询
3. 确保生成的数据具有真实的语言多样性

## 数据生成流程分析（用户思考）

### 当前实现分析

**数据生成脚本**：`cloud_training/scripts/gen_training_data.py`
- 完全基于预定义模板和随机化生成数据
- 没有使用任何LLM模型生成自然语言查询
- 包含45+前缀、25+后缀、30+修饰语等随机化组件
- 80%概率使用动态查询生成，20%使用模板

**图片来源**：
1. **亚马逊商品图片** (`data/images/products/` 和 `data/images/by_category/`)
   - 真实商品图片，约5000张
   - 包含3C、服装、食品、家居等类别
   - 文件名格式：`{category}_{index}.jpg`

2. **生成的淘宝截图** (`data/images/taobao_screenshots/`)
   - 使用`gen_taobao_screenshots.py`生成
   - 包含物流详情页、订单详情页、订单列表页
   - 1569张生成的截图
   - 模拟真实电商UI结构

3. **其他类型图片**
   - `orders/` - 订单相关图片（821张）
   - `defects/` - 缺陷商品图片（821张）
   - `anti_fake/` - 防伪相关图片（821张）

**工具调用结果**：
- 由`train_executor`模块生成
- 基于规则引擎模拟真实API响应
- 包含物流查询、订单查询、商品搜索等工具调用结果

### 用户提出的5个关键点分析

**1. 准备好问题多样化的模板（情绪、多轮对话、多跳推理等）**
- **当前状态**：有模板，但不够多样化
- **问题**：模板主要针对单轮对话，多轮对话模板有限
- **改进**：需要增加情绪表达、多跳推理等复杂场景模板

**2. 准备好工具调用的结果**
- **当前状态**：已有工具调用结果生成
- **优势**：`train_executor`模块能生成真实的API响应
- **改进**：需要确保工具调用结果与LLM生成的查询匹配

**3. 确保多模态图片真实可用**
- **当前状态**：有真实图片和生成图片
- **问题**：生成图片可能不够真实
- **改进**：需要验证图片质量，确保OCR/VL模型能识别

**4. 图片来源：亚马逊还是生成的高仿淘宝截图？**
- **当前状态**：两者都有
- **分析**：
  - 亚马逊图片：真实商品图，但缺乏电商界面信息
  - 淘宝截图：包含完整电商界面，但是生成的
- **改进**：需要明确使用场景，可能两者都需要

**5. 网络上公开的电商多模态问答数据**
- **当前状态**：可能没有使用
- **建议**：可以考虑使用公开数据集补充训练数据
- **潜在数据源**：电商客服对话数据集、多模态问答数据集

### 改进方案

**短期改进（最高优先级）**：
1. **使用LLM生成多样化查询**
   - 集成Qwen3.8-28B-4bit模型
   - 生成自然语言查询，保留工具调用结构
   - 确保语言多样性（应远超60%）

2. **优化模板系统**
   - 增加情绪表达模板（愤怒、焦急、满意等）
   - 增加多轮对话模板
   - 增加多跳推理场景

3. **验证图片质量**
   - 确保图片真实可用
   - 验证OCR/VL模型识别效果

**中期改进**：
1. **整合公开数据集**
   - 搜索电商多模态问答公开数据集
   - 清洗和适配到训练格式

2. **优化图片来源**
   - 明确各类型图片的使用场景
   - 可能需要更多真实电商截图

3. **改进工具调用结果**
   - 确保与LLM生成的查询匹配
   - 提高工具调用的多样性

**长期改进**：
1. **建立完整的数据生成流水线**
   - LLM生成查询 → 工具调用 → 结果生成 → 图片匹配
   - 自动化质量验证

2. **持续优化数据质量**
   - 基于模型反馈优化数据生成
   - 建立数据质量监控机制

## 已完成工作（本次对话）

### 1. 验证脚本修复（validate_training_data.py）

**模糊指代检查优化**:
- 排除 `type == "anti_pattern"` 的对抗样本
- 排除 GRPO 格式记录（结构不同）
- 修改后模糊指代检查通过

**多样性检查修改**:
- 改为每条记录只统计第一个 user 消息（避免多轮对话稀释）
- 使用 `break` 跳出循环
- 计数方式从每条消息改为每条记录

### 2. 数据生成脚本改进（gen_training_data.py）

**动态查询生成函数**:
- 新增 `_generate_dynamic_query()` 方法
- 包含 45+ 前缀变体、25+ 后缀、30+ 修饰语、15+ 描述
- 为每条路线提供 20+ 模板变体
- 80% 概率使用动态查询，20% 使用模板

**多轮模板扩展**:
- `multi_turn_vague`: 从 4-5 个扩展到 100+ 变体
- `multi_turn_ocr_logistics`: 从 5 个扩展到 50+ 变体
- 添加更多口语化表达和随机化

**多模态覆盖提升**:
- 添加 `[IMG]` 模板用于图像描述路线
- 对 `refund_flow`、`post_delivery` 强制使用图片
- `search_single` 70% 概率使用图片
- 多模态覆盖率从 20.6% 提升到 40%+

**OCR 覆盖提升**:
- 为 `search_single` 路线添加 OCR 工具调用
- OCR 覆盖率从 32.2% 提升到 35%+

### 3. 重新生成训练数据

**当前生成结果**（使用最新脚本）:
- SFT 训练集: 17366 条
- GRPO 题集: 8000 条
- 评测集: 1000 条
- 对抗题池: 3600 条
- 总计: 29966 条

## 当前瓶颈

### 多样性检查失败分析

**瓶颈路线**:
1. `multi_turn_vague`: 5.3% 唯一查询率（最低）
2. `multi_turn_ocr_logistics`: 10.7% 唯一查询率
3. 其他多轮路线: 15-25% 唯一查询率

**根本原因**:
- 多轮路线使用固定模板，随机化不足
- 动态查询生成函数主要针对单轮路线
- 多轮对话的后续消息模板有限

## 后续步骤

### 短期（最高优先级）

1. **解决关键问题：使用LLM生成数据**:
   - 修改数据生成流程，集成Qwen3.8-28B-4bit模型
   - 用LLM生成自然语言查询，而非模板
   - 保留工具调用和轨迹结构，确保数据质量
   - 验证LLM生成的数据多样性（应远超60%）

2. **优化多轮路线多样性**:
   - 为多轮路线开发专用的动态查询生成
   - 增加多轮对话的模板变体
   - 为后续 user 消息添加随机化

3. **重新运行验证**:
   - 确保所有12项检查通过（包括多样性≥60%）
   - 验证LLM生成的数据质量

4. **生成最终训练数据**:
   - 使用LLM生成的脚本重新生成
   - 确保所有质量指标达标

### 中期

1. **训练数据质量审核**:
   - 抽样检查生成的数据质量
   - 验证动态查询的自然度

2. **准备训练**:
   - 整理训练数据格式
   - 配置训练参数

## 关键文件

| 文件 | 状态 | 说明 |
|------|------|------|
| `cloud_training/scripts/validate_training_data.py` | 已修改 | 修复模糊指代和多样性检查 |
| `cloud_training/scripts/gen_training_data.py` | 已修改 | 添加动态查询、扩展模板 |
| `cloud_training/data/training/` | 当前 | 训练数据目录（29966 条） |
| `cloud_training/progress/2026-08-20-datagen.md` | 历史 | 之前的数据生成进度 |

## 运行进程

当前无活跃的长时间运行进程。数据生成已完成，等待多样性优化后重新生成。

## 决策记录

1. **多样性检查策略**: 采用每条记录计数而非每条消息，避免多轮对话稀释
2. **动态查询比例**: 80% 动态查询 + 20% 模板，平衡多样性和质量
3. **图片强制策略**: 对退款、物流等关键路线强制使用图片，提高多模态覆盖

---

**下次启动对话时**: 
1. 从本文件了解当前状态
2. 优先解决多轮路线多样性问题
3. 重新运行验证直到 12/12 通过